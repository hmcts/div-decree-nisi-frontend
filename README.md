# Divorce Decree Nisi

## Getting started


Install dependencies:

`yarn install`

Start database:

`docker-compose up`

Start application:

`yarn dev`

The application will now be running on https://localhost:3000
