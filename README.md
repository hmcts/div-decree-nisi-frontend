# Divorce Decree Nisi

## Getting started

Install dependencies:

`yarn install`

Start application:

`docker-compose up`

`yarn dev`

The application will now be running on https://localhost:3000

Test application:


`yarn test`
